#write a code to move all the zeros to the end of an array 
#apporach 
arr= list(map(int,input("Enter elements: ").split()))
result=[]
zeros= 0
for num in arr:
    if num !=0:
        result.append(num)
    else:
        zeros+= 1
result.extend([0]* zeros)
print("Result", result)