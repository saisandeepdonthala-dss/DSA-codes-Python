'''Calculate the height of a tree and Count of nodes in a tree '''
class node:
    def __init__(self, data):
        self.data= data
        self.left= None
        self.right= None
def count_nodes(root):
    if not root:
        return 0
    return 1+ count_nodes(root.left) + count_nodes(root.right)
def height(root):
    if not root:
        return 0
    return 1 + max(height(root.left), height(root.right))
root = node(int(input("Enter root: ")))
root.left = node(int(input("Enter left: ")))
root.right = node(int(input("Enter right: ")))
root.left.right= node(int(input("Enter left-right: ")))
print("Number of nodes in the tree is:", count_nodes(root))
print("Height of the tree is:", height(root))